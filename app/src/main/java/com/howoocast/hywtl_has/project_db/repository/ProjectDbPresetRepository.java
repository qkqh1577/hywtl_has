package com.howoocast.hywtl_has.project_db.repository;

import com.howoocast.hywtl_has.project_db.domain.ProjectDbPreset;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProjectDbPresetRepository extends JpaRepository<ProjectDbPreset, Long> {
}
